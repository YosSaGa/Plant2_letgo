import { useEffect, useMemo, useRef, useState } from 'react';
import { AnimatePresence, motion, useReducedMotion } from 'framer-motion';
import { Area, AreaChart, ResponsiveContainer, Tooltip } from 'recharts';
import { CircleMarker, MapContainer, TileLayer, Tooltip as LeafletTooltip, useMap } from 'react-leaflet';
import { ArrowLeft, LocateFixed, MapPinned, Users } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { userLocations } from './mockDashboardData';
import './admin-user-map.css';
import './admin-polish.css';
const format = (value) => new Intl.NumberFormat('th-TH').format(value);
const defaultCenter = [13.4, 100.3]; const days = ['จ.', 'อ.', 'พ.', 'พฤ.', 'ศ.', 'ส.', 'อา.'];
function FlyToLocation({ location, reduceMotion }) { const map = useMap(); useEffect(() => { if (location) map.flyTo(location.position, 8, { duration: reduceMotion ? 0 : .75 }); }, [location, map, reduceMotion]); return null; }
function MapReady({ mapRef, reduceMotion }) { const map = useMap(); useEffect(() => { mapRef.current = map; const timer = window.setTimeout(() => { map.invalidateSize(); if (!reduceMotion) map.flyTo(userLocations[0].position, 7.3, { duration: 1.2 }); }, 120); return () => window.clearTimeout(timer); }, [map, mapRef, reduceMotion]); return null; }
export default function AdminUserMap() {
  const navigate = useNavigate(); const reduceMotion = useReducedMotion(); const [selected, setSelected] = useState(null); const mapRef = useRef(null);
  const locations = useMemo(() => [...userLocations].sort((a, b) => b.users - a.users), []); const chartData = selected?.trend.map((total, index) => ({ day: days[index], total }));
  return <main className="aum-root"><header className="aum-header"><button onClick={() => navigate('/admin/dashboard')}><ArrowLeft size={19} /> กลับแดชบอร์ด</button><div><p>ADMIN MOCKUP</p><h1>แผนที่การกระจายตัวของผู้ใช้งาน</h1></div></header><div className="aum-layout"><section className="aum-map-wrap"><MapContainer center={defaultCenter} zoom={5.4} scrollWheelZoom className="aum-map"><TileLayer attribution="&copy; OpenStreetMap contributors" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/><MapReady mapRef={mapRef} reduceMotion={reduceMotion}/><FlyToLocation location={selected} reduceMotion={reduceMotion}/>{locations.map((location) => <CircleMarker key={location.name} center={location.position} radius={selected?.name === location.name ? Math.max(14, location.users / 35) : Math.max(9, location.users / 45)} pathOptions={{ color: selected?.name === location.name ? '#022c22' : '#047857', weight: selected?.name === location.name ? 3 : 1.5, fillColor: '#10b981', fillOpacity: .7, className: location.name === userLocations[0].name && !reduceMotion ? 'map-pulse' : '' }} eventHandlers={{ click: () => setSelected(location) }}><LeafletTooltip>{location.name} · {format(location.users)} ผู้ใช้งาน</LeafletTooltip></CircleMarker>)}</MapContainer><button className="aum-reset" onClick={() => mapRef.current?.flyTo(defaultCenter, 5.4, { duration: reduceMotion ? 0 : .75 })}><LocateFixed size={17} /> รีเซ็ตมุมมอง</button><span className="aum-map-label"><MapPinned size={17} /> ประเทศไทย · ข้อมูลจำลอง</span></section><aside className="aum-panel"><AnimatePresence mode="wait">{selected ? <motion.div key={selected.name} initial={{ opacity: 0, x: reduceMotion ? 0 : 15 }} animate={{ opacity: 1, x: 0 }} exit={{ opacity: 0, x: reduceMotion ? 0 : -15 }}><button className="aum-list-back" onClick={() => setSelected(null)}>← ดูรายชื่อทั้งหมด</button><p className="aum-kicker">จังหวัดที่เลือก</p><h2>{selected.name}</h2><div className="aum-user-total"><Users size={22} /><div><span>ผู้ใช้งานทั้งหมด</span><strong>{format(selected.users)} คน</strong></div></div><h3>แนวโน้มการเข้าใช้ 7 วันล่าสุด</h3><ResponsiveContainer width="100%" height={210}><AreaChart data={chartData}><defs><linearGradient id="locationFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#10b981" stopOpacity={.4}/><stop offset="100%" stopColor="#10b981" stopOpacity={0}/></linearGradient></defs><Tooltip/><Area type="monotone" dataKey="total" stroke="#059669" strokeWidth={3} fill="url(#locationFill)" isAnimationActive={!reduceMotion} animationDuration={800}/></AreaChart></ResponsiveContainer><div className="aum-detail-cards"><p><span>การเติบโตสัปดาห์นี้</span><b>+{Math.round((selected.trend[6] - selected.trend[0]) / selected.trend[0] * 100)}%</b></p><p><span>วันที่เข้าใช้มากที่สุด</span><b>วันเสาร์</b></p></div></motion.div> : <motion.div key="list" initial={{ opacity: 0, x: reduceMotion ? 0 : 15 }} animate={{ opacity: 1, x: 0 }} exit={{ opacity: 0, x: reduceMotion ? 0 : -15 }}><p className="aum-kicker">ผู้ใช้งานตามพื้นที่</p><h2>เลือกจังหวัดเพื่อดูรายละเอียด</h2><p className="aum-intro">คลิก marker บนแผนที่ หรือเลือกรายการจากด้านล่าง</p><div className="aum-location-list">{locations.map((location, index) => <motion.button key={location.name} onClick={() => setSelected(location)} whileHover={reduceMotion ? undefined : { y: -4, boxShadow: '0 12px 24px -8px rgba(6,78,59,0.18)' }} whileTap={{ scale: .98 }}><b>{index + 1}</b><span>{location.name}<small>ผู้ใช้งานในระบบ</small></span><strong>{format(location.users)}</strong></motion.button>)}</div></motion.div>}</AnimatePresence></aside></div></main>;
}
